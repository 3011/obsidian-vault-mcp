apiVersion: v1
kind: Service
metadata:
  name: obsidian-vault-mcp
  labels:
    app: obsidian-vault-mcp
    profile: personal-full-access
spec:
  type: ClusterIP
  selector:
    app: obsidian-vault-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
